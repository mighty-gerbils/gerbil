# Regular Expressions

::: tip usage
(import :std/pregexp)
:::

The well-known portable Scheme regular expressions library.

```
(import :std/pregexp)

;; exports:

  pregexp
  pregexp-match-positions
  pregexp-match
  pregexp-split
  pregexp-replace
  pregexp-replace*
  pregexp-quote
```