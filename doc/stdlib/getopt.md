# GetOpt
::: tip usage
(import :std/getopt)
:::

Command-line argument parsing.

```
(import :std/getopt)

;; exports:

  getopt
  getopt? getopt-object?
  getopt-error?
  getopt-parse
  getopt-display-help
  option
  flag
  command
  argument
  optional-argument
  rest-arguments
```