# String Templates

The `:std/misc/template` library provides basic string templating procedures. This templating
interface is much simpler than something like [the `format` procedure from the `:std/format`
module](/reference/std/format.md#format).

::: tip To use the bindings from this module:
```scheme
(import :std/misc/template)
```
:::
