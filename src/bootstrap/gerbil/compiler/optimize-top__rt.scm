(declare (block) (standard-bindings) (extended-bindings))
(begin
  (begin
    (load-module "gerbil/expander__rt")
    (load-module "gerbil/compiler/base__rt")
    (load-module "gerbil/compiler/compile__rt")
    (load-module "gerbil/compiler/optimize-base__rt")
    (load-module "gerbil/compiler/optimize-xform__rt"))
  (load-module "gerbil/compiler/optimize-top__0"))
