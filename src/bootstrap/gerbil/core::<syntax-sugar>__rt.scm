(declare (block) (standard-bindings) (extended-bindings))
(begin
  (begin)
  (begin
    (load-module "gerbil/core::<expander-runtime>__rt")
    (load-module "gerbil/core::<syntax-case>__rt"))
  (begin)
  (begin)
  (begin))
