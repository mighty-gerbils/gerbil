;;; -*- Gerbil -*-
;;; © vyzo
;;; ensemble network implementation
(import :std/error
        ../interface
        ../config)
(export new-network)

(def (new-network (sec-ctx : SecurityContext)
                  (evt-bus : EventBus)
                  (limits : Limits))
  (TODO new-network))
