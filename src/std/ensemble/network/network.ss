;;; -*- Gerbil -*-
;;; © vyzo
;;; ensemble network implementation
(import :std/error
        ../interface
        ../config)
(export new-network)

(def (new-network (host : Host) (limits : NetworkLimits))
  (TODO new-network))
