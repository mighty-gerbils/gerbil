;;; -*- Gerbil -*-
;;; © vyzo
;;; ensemble broadcast implementation
(import :std/error
        ../interface
        ../config)
(export new-broadcast)

(def (new-broadcast (host : Host)
                    (limits : Limits))
  (TODO new-broadcast))
